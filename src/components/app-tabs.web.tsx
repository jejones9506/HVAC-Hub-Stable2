// DISABLED - Navigation is now dropdown-only from Home (and pure Stack in _layout.tsx).
// Bottom tabs completely removed per requirements. All sections (Home, Equipment, Calculators, AI Assistant, EPA Study, Profile, Admin, TOS, Terminology, Diagrams) are accessible ONLY via the ☰ Menu dropdown on the Home screen.
// This file is kept as a stub to avoid import errors during builds. Do not import or use AppTabs anywhere.
import React from 'react';
export default function AppTabs() {
  return null; // No tabs rendered - pure stack + dropdown navigation only
}
